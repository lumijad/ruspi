{
  "pages": [
    {
      "name": "index",
      "params": {
        "layout": "components/app-layout",
        "title": "Ruspi",
        "description": "ruspi.dev is an experimental website used written in the programming language rust"
      }
    },
    {
      "name": "hello-world",
      "params": {
        "layout": "components/app-layout",
        "title": "Hello World based on the warp web framework.",
        "description": "Hello World, written in the ruts programming language. Based on the web server framework warp."
      }
    },
    {
      "name": "complex-set-of-routes",
      "params": {
        "layout": "components/app-layout",
        "title": "Complex set of routes for the warp framework.",
        "description": "A simple web server written in the ruts programming language. Based on the web server framework warp."
      }
    },
    {
      "name": "jwt",
      "params": {
        "layout": "components/app-layout",
        "title": "JSON Web Token",
        "description": "JSON Web-Token authorization with the warp web server framework."
      }
    },
    {
      "name": "tls",
      "params": {
        "layout": "components/app-layout",
        "title": "Transport layer security",
        "description": "Transport layer security (TLS) with the warp web server framework."
      }
    },
    {
      "name": "about",
      "params": {
        "layout": "components/app-layout",
        "title": "Ruspi Webserver",
        "description": "ruspi.dev is an experimental website used written in the programming language rust"
      }
    },
    {
      "name": "error_404",
      "params": {
        "layout": "components/app-layout",
        "title": "Ruspi Webserver: Page not found",
        "description": "ruspi.dev is an experimental website used written in the programming language rust"
      }
    }
  ]
}